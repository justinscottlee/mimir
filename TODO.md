- On the tabs in the main content area, add a plus button (forced all the way right--just after the last tab on the right), that when clicked, opens a small dropdown with for now, two options: New conversation, New workspace. Clicking either of those obviously creates a new tab for that and opens it. (ai task)
- Add a button on the panel that is displayed for tool use on "remember" whenever it is expanded. This button will let you delete the memory the AI decided to make fron the chat without needing to go back to the memories window. (ai task)
- Make it so the pop-out windows (like Memories, Skills, etc.) are resizeable (with a window-dependent minimum size, and a window-dependent default size, and a window-dependent maximum size). (ai task)
- add web search and web fetch tools (major ai task, use SEARXNG?)
- add backend database for persistent storage. likely postgresql. maybe redis as well? (major ai task maybe wait for fable 5 again)
- implement user authentication and access controls (major ai task maybe wait for fable 5 again)
- add a way to customize the system prompt? perhaps in a way that constructs a larger system prompt from smaller individual ones (like the memories or skills knowledge) (ai will do this, need to detail how it will work)
- update all icons (human review capable)
- revamp conversations list window. add a confirmation for deleting(make reusable if other things could?). add ability to select multiple (so you may select multiple to delete). add a search bar at top of conversations window (use same code as already existing search feature)
- add a setting for selecting the default model to use.
- add a setting for importing multiple model endpoints and rendering the model selection in a way grouped by endpoint, maybe with buttons to minimize/expand?
- add tasks, this is somewhat agentic, maybe wait until the basics of workspace are implemented? have tasks run on a schedule, basically gives a agentic session a prompt to run periodically and may alter states I guess? for example: one that is intended to consolidate and delete duplicate or contradictory memories
- whenever a message is interrupted (stopped during generation), put some text at the end of the message or near it something that indicated it was interrupted
- fix resend button